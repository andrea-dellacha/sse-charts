export type SocialPostMetadata = {
  key: string;
  color: string;
  label: string;
};

export type SocialPostType = {
  id: number;
  timestamp: number;
};

export type BubbleChartItem = {
  x: number;
  y: number;
  r: number;
};
